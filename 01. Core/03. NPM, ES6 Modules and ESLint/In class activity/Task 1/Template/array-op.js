const removeFirst = (arr) => {
  if (!Array.isArray(arr)) {
    throw new Error(`${arr} is not an Array!`);
  }

}

const removeLast = (arr) => {
 
};

const addFirst = (arr, element) => {
 
};

const addLast = (arr, element) => {
   
};

const contains = (arr, element) => {
 
};
